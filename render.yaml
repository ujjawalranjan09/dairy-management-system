databases:
  # Free PostgreSQL database on Render
  - name: dairy-db
    plan: free
    databaseName: dairy
    user: dairy

services:
  # Backend Web Service
  - type: web
    name: dairy-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && node node_modules/prisma/build/index.js generate
    startCommand: node node_modules/prisma/build/index.js db push --accept-data-loss && npm start
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: dairy-db
          property: connectionString

  # Frontend Static Site
  - type: web
    name: dairy-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        sync: false
